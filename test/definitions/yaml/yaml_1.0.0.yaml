info:
  version: 1.0.0
  title: yaml 
  description: general yaml laptop test
basePath: /laptop
swagger: '2.0'
paths:
  /echo:
    post:
      responses:
        '200':
          description: 200 OK
x-ibm-configuration:
  assembly:
    execute:
      - invoke:
          title: invoke
          target-url: 'http://localhost:8889/'
schemes:
  - http