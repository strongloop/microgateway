swagger: "2.0"

info:
  version: 1.0.0
  title: OAuth Resource Test
  x-ibm-name: oauth-resource-test
  description: OAuth Protected API
host: host.com
basePath: /resource-test
paths:
  /res1:
    get:
      responses:
        '200':
          description: 200 OK
  /res3:
    get:
      responses:
        '200':
          description: 200 OK
definitions: {}
schemes:
  - https
securityDefinitions:
  ClientID:
    in: query
    name: client_id
    type: apiKey
  OAuthDef:
    type: oauth2
    authorizationUrl: ''   #doc only
    flow: implicit accessCode  #doc only
    scopes:
      '/': default root scope
security:
  - OAuthDef: [ '/test600' ]
  - ClientID: []
x-ibm-configuration:
  enforced: true
  phase: realized
  testable: true
  cors:
    enabled: true
  #assembly:
  #  execute:
  #    - proxy:
  #        title: proxy
  #        target-url: 'http://www.google.com/'
