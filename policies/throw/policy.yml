policy: 1.1.0

info:
  title: Throw
  name: throw
  version: 1.0.0
  description: Throw policy for the IBM API Connect
  contact:
    name: IBM API Mesh Samples
    url: https://www.ibm.com/apimesh
    email: steve-product-manager@ibm.com

properties:
  type: "object"
  properties:
    name:
      label: Error name
      description: The name of the error to be thrown
      type: string
    message:
      label: Error message
      description: The message to be sent with the error
      type: string
  required:
    - name
  throw:
    - '*'
